% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-methods.R

\name{plot.statderive}
\alias{plot.statderive}
\title{Plot method for statderive objects}
\usage{
\method{plot}{statderive}(x, y = NULL, ...)
}
\arguments{
\item{x}{Object of class "statderive"}

\item{y}{Unused (required for S3 plot method signature)}

\item{...}{Additional graphical parameters passed to plotting functions}
}
\value{
Invisibly returns the input object \code{x} (of class \code{statderive}).
This function is called primarily for its side effect of producing plots
that compare simulated data with theoretical probability density functions.
}
\description{
Creates a visualization comparing empirical (simulated) results with theoretical
predictions. Displays a histogram of simulated values with an overlay of the
theoretical probability density function.
}
\details{
The plot requires that \code{\link{simulate_check}} has been run on the object first.
The visualization includes:
\itemize{
  \item Histogram of simulated values
  \item Theoretical PDF curve overlay
  \item Legend distinguishing empirical from theoretical
  \item Statistics box showing mean and variance comparison
}
}
\examples{
\dontrun{
obj <- derive_transform(
  dist = "uniform",
  params = list(min = 0, max = 1),
  transform = "x^2"
)
obj_sim <- simulate_check(obj, n_sim = 5000)
plot(obj_sim)
}
}

